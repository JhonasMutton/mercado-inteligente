
# configure your datasource
quarkus:
  datasource:
    db-kind : mysql
    username : root
    password : admin
    jdbc:
      url : jdbc:mysql://localhost:3306/mercadointeligente

  # drop and create the database at startup (use `update` to only update the schema)
  hibernate-orm:
    database:
      generation: drop-and-create